@using Microsoft.AspNetCore.Identity
@model VibeWave.Models.LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient m-lg-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Login</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" asp-route-returnurl="@ViewBag.ReturnUrl" class="row">
            <div class="border p-3">
                <div asp-validation-summary="All" class="text-danger"></div>

                <div class="mb-3 row p-1">
                    <label asp-for="Email" class="ms-2"></label>
                    <input asp-for="Email" class="form-control border-0 shadow"></input>
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="mb-3 row p-1">
                    <label asp-for="Password" class="ms-2"></label>
                    <input asp-for="Password" class="form-control border-0 shadow"></input>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="mb-3 row p-1">
                    <div class="form-check">
                        <input asp-for="RememberMe" class="form-check-input"></input>
                        <label asp-for="RememberMe" class="form-check-label"></label>
                    </div>
                </div>

                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        <button type="submit" class="btn btn-primary form-control">Login</button>
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-action="Register" class="btn btn-primary form-control">Register</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}